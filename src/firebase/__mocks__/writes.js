export const postCompetitor = jest.fn(competitor => true)

export const postWorkout = jest.fn(workout => true)

export const postScores = jest.fn(scores => true)

export const updateCompetitor = jest.fn((url, competitor) => true)

export const updateWorkout = jest.fn((url, workout) => true)
